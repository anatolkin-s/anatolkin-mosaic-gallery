# Intentionally inactive.
#
# Historical PageTS that registered a CType=list / list_type=mosaicgallery_pi1
# New Content Element wizard entry. It must NOT be loaded via addPageTSConfig().
#
# Canonical creation UI is the dedicated CType=mosaicgallery_pi1 content element
# registered by ExtensionUtility::registerPlugin() into the Gallery group.
# Legacy list_type signatures remain in static TYPO3 13 TCA for DataHandler
# validity and are filtered from FormEngine creation/edit UI by
# MosaicGalleryLegacyListTypeVisibilityProvider.
